# 하오웹 — 임시 미리보기

이 저장소는 **빌드 결과물 보관용**이다. 소스는 비공개 저장소 `haoweb-site` 에 있고,
여기 있는 파일은 `_build_dist.ps1` 이 만든 공개 빌드를 그대로 옮긴 것이다.
직접 고치지 말 것 — 다음 배포 때 통째로 덮어쓴다.

미리보기: https://seyeong1403.github.io/haoweb-preview/

검토 단계라 `robots.txt` 는 전체 차단이고 모든 페이지에 `noindex` 가 걸려 있다.